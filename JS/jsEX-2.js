/*
scope in js -
    1. global  : without any declaration
    2. function: var
    3. block   : let
    4. value doesnt change : const

    x
    var y =3
    function fn(){
        {
            y=5
        }
        {
            let z
        }
    //
    }


    always declare a global var with var keyword,
    as browsers now are structly global so they 
    only allow global variables with var
    not directly.


    BOM - Browser Object Model
        access the values related to a broswer
        window (object) - has parameters and methods\
        

    */